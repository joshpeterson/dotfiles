- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/.lldbinit: lldbinit
    ~/.zshrc: zshrc
    ~/.config:
        path: config/
        create: true
    ~/.claude/settings.json:
        path: config/claude/settings.json
        create: true


