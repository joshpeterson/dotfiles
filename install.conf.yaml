- clean:
    ~/:
      force: true
    ~/.config:
      force: true
      recursive: true

- link:
    ~/.gitconfig: gitconfig
    ~/.tmux.conf: tmux.conf
    ~/bin:
        path: bin/
        create: true
    ~/.config:
        path: config/
        create: true


